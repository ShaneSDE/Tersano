{
  "name": "ts-mern-",
  "version": "1.0.0",
  "description": "## Lessons",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\"  \"npm run dev --prefix frontend\" ",
    "build": "cd backend && npm install && npm run build  && cd ../frontend && npm install && npm run build",
    "start": "node backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/basir/ts-mern-.git"
  },
  "author": "Shane",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/basir/ts-mern-/issues"
  },
  "homepage": "https://github.com/basir/ts-mern-#readme",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
